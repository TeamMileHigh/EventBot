import pino from "pino";
export function createLogger(useJson, level, name, metadata) {
    return pino({
        name,
        level,
        ...(!useJson
            ? {
                transport: {
                    target: "pino-pretty",
                },
            }
            : {}),
    }).child(metadata || {});
}
//# sourceMappingURL=logger.js.map