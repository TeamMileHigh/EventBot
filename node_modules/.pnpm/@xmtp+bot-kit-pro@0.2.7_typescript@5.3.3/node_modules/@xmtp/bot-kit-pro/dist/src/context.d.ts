import { DecodedMessage, SendOptions } from "@xmtp/xmtp-js";
import { Json } from "./types.js";
type PreparedReply = {
    content: any;
    options?: SendOptions;
};
export default class HandlerContext<ConversationState = Json, BotState = Json> {
    message: DecodedMessage;
    conversationState: ConversationState;
    botState: BotState;
    preparedReplies: PreparedReply[];
    reply: (content: any, options?: SendOptions) => void;
    constructor(message: DecodedMessage, conversationState: ConversationState, botState: BotState);
}
export {};
//# sourceMappingURL=context.d.ts.map